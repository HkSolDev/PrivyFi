/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findUserRewardPda } from "../pdas";
import { PRIVYFI_PROGRAM_ADDRESS } from "../programs";

export const RECORD_ACTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  153, 153, 235, 171, 52, 54, 196, 145,
]);

export function getRecordActionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RECORD_ACTION_DISCRIMINATOR,
  );
}

export type RecordActionInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountUser extends string | AccountMeta<string> = string,
  TAccountUserReward extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountUser extends string
        ? WritableSignerAccount<TAccountUser> & AccountSignerMeta<TAccountUser>
        : TAccountUser,
      TAccountUserReward extends string
        ? WritableAccount<TAccountUserReward>
        : TAccountUserReward,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RecordActionInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type RecordActionInstructionDataArgs = { amount: number | bigint };

export function getRecordActionInstructionDataEncoder(): FixedSizeEncoder<RecordActionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: RECORD_ACTION_DISCRIMINATOR }),
  );
}

export function getRecordActionInstructionDataDecoder(): FixedSizeDecoder<RecordActionInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getRecordActionInstructionDataCodec(): FixedSizeCodec<
  RecordActionInstructionDataArgs,
  RecordActionInstructionData
> {
  return combineCodec(
    getRecordActionInstructionDataEncoder(),
    getRecordActionInstructionDataDecoder(),
  );
}

export type RecordActionAsyncInput<
  TAccountUser extends string = string,
  TAccountUserReward extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userReward?: Address<TAccountUserReward>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: RecordActionInstructionDataArgs["amount"];
};

export async function getRecordActionInstructionAsync<
  TAccountUser extends string,
  TAccountUserReward extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: RecordActionAsyncInput<
    TAccountUser,
    TAccountUserReward,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RecordActionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserReward,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userReward: { value: input.userReward ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.userReward.value) {
    accounts.userReward.value = await findUserRewardPda({
      user: getAddressFromResolvedInstructionAccount(
        "user",
        accounts.user.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("userReward", accounts.userReward),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordActionInstructionDataEncoder().encode(
      args as RecordActionInstructionDataArgs,
    ),
    programAddress,
  } as RecordActionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserReward,
    TAccountSystemProgram
  >);
}

export type RecordActionInput<
  TAccountUser extends string = string,
  TAccountUserReward extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  user: TransactionSigner<TAccountUser>;
  userReward: Address<TAccountUserReward>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: RecordActionInstructionDataArgs["amount"];
};

export function getRecordActionInstruction<
  TAccountUser extends string,
  TAccountUserReward extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: RecordActionInput<
    TAccountUser,
    TAccountUserReward,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RecordActionInstruction<
  TProgramAddress,
  TAccountUser,
  TAccountUserReward,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    user: { value: input.user ?? null, isWritable: true },
    userReward: { value: input.userReward ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("user", accounts.user),
      getAccountMeta("userReward", accounts.userReward),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRecordActionInstructionDataEncoder().encode(
      args as RecordActionInstructionDataArgs,
    ),
    programAddress,
  } as RecordActionInstruction<
    TProgramAddress,
    TAccountUser,
    TAccountUserReward,
    TAccountSystemProgram
  >);
}

export type ParsedRecordActionInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    user: TAccountMetas[0];
    userReward: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: RecordActionInstructionData;
};

export function parseRecordActionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRecordActionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      user: getNextAccount(),
      userReward: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRecordActionInstructionDataDecoder().decode(instruction.data),
  };
}

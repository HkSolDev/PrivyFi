/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getArrayDecoder,
  getArrayEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findYieldStorePda } from "../pdas";
import { PRIVYFI_PROGRAM_ADDRESS } from "../programs";
import {
  getYieldDataDecoder,
  getYieldDataEncoder,
  type YieldData,
  type YieldDataArgs,
} from "../types";

export const UPDATE_YIELDS_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  115, 184, 90, 132, 80, 44, 196, 201,
]);

export function getUpdateYieldsDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_YIELDS_DISCRIMINATOR,
  );
}

export type UpdateYieldsInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountYieldStore extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountYieldStore extends string
        ? WritableAccount<TAccountYieldStore>
        : TAccountYieldStore,
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateYieldsInstructionData = {
  discriminator: ReadonlyUint8Array;
  strategies: Array<YieldData>;
};

export type UpdateYieldsInstructionDataArgs = {
  strategies: Array<YieldDataArgs>;
};

export function getUpdateYieldsInstructionDataEncoder(): Encoder<UpdateYieldsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["strategies", getArrayEncoder(getYieldDataEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_YIELDS_DISCRIMINATOR }),
  );
}

export function getUpdateYieldsInstructionDataDecoder(): Decoder<UpdateYieldsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["strategies", getArrayDecoder(getYieldDataDecoder())],
  ]);
}

export function getUpdateYieldsInstructionDataCodec(): Codec<
  UpdateYieldsInstructionDataArgs,
  UpdateYieldsInstructionData
> {
  return combineCodec(
    getUpdateYieldsInstructionDataEncoder(),
    getUpdateYieldsInstructionDataDecoder(),
  );
}

export type UpdateYieldsAsyncInput<
  TAccountYieldStore extends string = string,
  TAccountAuthority extends string = string,
> = {
  yieldStore?: Address<TAccountYieldStore>;
  authority: TransactionSigner<TAccountAuthority>;
  strategies: UpdateYieldsInstructionDataArgs["strategies"];
};

export async function getUpdateYieldsInstructionAsync<
  TAccountYieldStore extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: UpdateYieldsAsyncInput<TAccountYieldStore, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateYieldsInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    yieldStore: { value: input.yieldStore ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.yieldStore.value) {
    accounts.yieldStore.value = await findYieldStorePda();
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("yieldStore", accounts.yieldStore),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateYieldsInstructionDataEncoder().encode(
      args as UpdateYieldsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateYieldsInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority
  >);
}

export type UpdateYieldsInput<
  TAccountYieldStore extends string = string,
  TAccountAuthority extends string = string,
> = {
  yieldStore: Address<TAccountYieldStore>;
  authority: TransactionSigner<TAccountAuthority>;
  strategies: UpdateYieldsInstructionDataArgs["strategies"];
};

export function getUpdateYieldsInstruction<
  TAccountYieldStore extends string,
  TAccountAuthority extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: UpdateYieldsInput<TAccountYieldStore, TAccountAuthority>,
  config?: { programAddress?: TProgramAddress },
): UpdateYieldsInstruction<
  TProgramAddress,
  TAccountYieldStore,
  TAccountAuthority
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    yieldStore: { value: input.yieldStore ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("yieldStore", accounts.yieldStore),
      getAccountMeta("authority", accounts.authority),
    ],
    data: getUpdateYieldsInstructionDataEncoder().encode(
      args as UpdateYieldsInstructionDataArgs,
    ),
    programAddress,
  } as UpdateYieldsInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority
  >);
}

export type ParsedUpdateYieldsInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    yieldStore: TAccountMetas[0];
    authority: TAccountMetas[1];
  };
  data: UpdateYieldsInstructionData;
};

export function parseUpdateYieldsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateYieldsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { yieldStore: getNextAccount(), authority: getNextAccount() },
    data: getUpdateYieldsInstructionDataDecoder().decode(instruction.data),
  };
}

/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findYieldStorePda } from "../pdas";
import { PRIVYFI_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_YIELD_STORE_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([157, 130, 156, 8, 147, 185, 255, 246]);

export function getInitializeYieldStoreDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_YIELD_STORE_DISCRIMINATOR,
  );
}

export type InitializeYieldStoreInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountYieldStore extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountYieldStore extends string
        ? WritableAccount<TAccountYieldStore>
        : TAccountYieldStore,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeYieldStoreInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeYieldStoreInstructionDataArgs = {};

export function getInitializeYieldStoreInstructionDataEncoder(): FixedSizeEncoder<InitializeYieldStoreInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: INITIALIZE_YIELD_STORE_DISCRIMINATOR,
    }),
  );
}

export function getInitializeYieldStoreInstructionDataDecoder(): FixedSizeDecoder<InitializeYieldStoreInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeYieldStoreInstructionDataCodec(): FixedSizeCodec<
  InitializeYieldStoreInstructionDataArgs,
  InitializeYieldStoreInstructionData
> {
  return combineCodec(
    getInitializeYieldStoreInstructionDataEncoder(),
    getInitializeYieldStoreInstructionDataDecoder(),
  );
}

export type InitializeYieldStoreAsyncInput<
  TAccountYieldStore extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  yieldStore?: Address<TAccountYieldStore>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeYieldStoreInstructionAsync<
  TAccountYieldStore extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: InitializeYieldStoreAsyncInput<
    TAccountYieldStore,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeYieldStoreInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    yieldStore: { value: input.yieldStore ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.yieldStore.value) {
    accounts.yieldStore.value = await findYieldStorePda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("yieldStore", accounts.yieldStore),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeYieldStoreInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeYieldStoreInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type InitializeYieldStoreInput<
  TAccountYieldStore extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  yieldStore: Address<TAccountYieldStore>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeYieldStoreInstruction<
  TAccountYieldStore extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof PRIVYFI_PROGRAM_ADDRESS,
>(
  input: InitializeYieldStoreInput<
    TAccountYieldStore,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeYieldStoreInstruction<
  TProgramAddress,
  TAccountYieldStore,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? PRIVYFI_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    yieldStore: { value: input.yieldStore ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("yieldStore", accounts.yieldStore),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeYieldStoreInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeYieldStoreInstruction<
    TProgramAddress,
    TAccountYieldStore,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeYieldStoreInstruction<
  TProgram extends string = typeof PRIVYFI_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    yieldStore: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeYieldStoreInstructionData;
};

export function parseInitializeYieldStoreInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeYieldStoreInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      yieldStore: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeYieldStoreInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
